# Unified release — 22 September 2026

Consolidates browser branch `5e5eb704e709f0fd83fff0cbfeff016281619ec0` and the preserved local clinical/privacy work on `codex/unified-patient-workflow`.

Clinical closure now records explicit outcomes independently of financial settlement. Overdue unprocessed visits require reviewed cancellation. Identity corrections retain reversible provenance. Consent, identity-verified rights requests and single-use patient export share the Privacy & rights workflow. The duplicate draft rights API is retired; national exchange transport is disabled pending complete validation.

Pre-migration recovery branch: Neon `pre-unified-release-20260922`, ID `br-icy-frost-auo2bh2a`, parent `main` / `br-calm-night-auqcrsjo`, created 2026-09-22 16:36 Nairobi, no expiry. Project `restless-shadow-00344753` retains six hours of history. This is a retained provider recovery point, not evidence of an independent restore drill or off-provider backup.

Production secrets remain inside Vercel. The controlled release job runs Prisma migrations and the scoped `release-privacy-role.mjs` definition sync; it assigns no users and changes no passwords. Normal application builds stay migration-free. The job is a separate unaliased production deployment, followed by the application deployment.

Validation before release: 234 unit tests, 43 migrations, type check, production build, 99 isolated API/database checks. 25 browser scenarios verified (24 passed in the full run, with the remaining selector corrected and its clinical discharge/identity-reversal scenario passing in a focused rerun).

DHA certification and outstanding external readiness gates are not claimed by this release.

## Database release evidence

The controlled release job `dpl_66q4SsCh8sksDQz8VCq884zAtUD5` succeeded at 13:44:52 UTC. It applied the five pending migrations, from `20260922120000_safe_registration_and_consent_lifecycle` through `20260923140000_clinical_closure_identity_history`, and synchronized the DPO role definition without assigning users. The existing application's health endpoint confirmed database connectivity and the new migration head afterward.

For the release runner, export the reviewed Git commit into an isolated directory, copy `platform/certification/release-job.vercel.json` to that copy's `platform/vercel.json`, and deploy the isolated root with the explicit existing project and `--prod --skip-domain`. A root CLI config override alone does not replace a nested project-root config. Never promote the static release-job deployment. Deploy the normal application from `platform/vercel.json` afterward.

The failed initial configuration attempt was removed. Deployment uploads now explicitly exclude local databases, private branding source, environment files and browser test artifacts.

## Published application

Application source commit: `b6122e28d33243a6b066296876046c19c6a807e7`.
Deployment: `dpl_75bd4DPTtJ4QG7TpysSi1CALv3mi` / `mwein-hmis-platform-ms7d0rpjj-mbitis-projects.vercel.app`.
Published URL: https://mwein-hmis-platform.vercel.app/.

The staged health probe returned HTTP 200 with the exact source commit and migration head. It was then promoted to the existing application URL. The public health verifier confirmed the same source/migration pair after promotion. The browser loaded the published sign-in page correctly; the prior session was no longer authenticated, so live authenticated workflows were not exercised. Authenticated clinical, privacy and role-boundary scenarios were verified against the isolated synthetic fixture.

The readiness probe deliberately returns HTTP 503 (`blocked`). Publication is for continued review; it does not approve clinical go-live or the outstanding governance gates. National exchange remains disabled.

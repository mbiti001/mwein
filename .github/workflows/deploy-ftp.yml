name: Deploy to cPanel via FTP

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT }}
          local-dir: ./
          server-dir: ${{ secrets.FTP_TARGET }}name: Deploy to cPanel via FTP
on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT }}
          local-dir: ./
          server-dir: ${{ secrets.FTP_TARGET }}
          exclude: |
            .git*
            .github/
            node_modules/
            README.md
            *.zip
          # set to true to delete remote files that are not present locally
          # set to false if you want to preserve other server files
cd /Users/useruser/Downloads/mwein
zip -r ../mwein-deploy.zip . -x ".git/*" -x "node_modules/*"
          git-ftp-args: --insecure
git add .github/workflows/deploy-ftp.yml
git commit -m "Add FTP deploy workflow"
git push origin git add .github/workflows/deploy-ftp.yml
git commit -m "Add FTP deploy workflow"
git push origin main
git log --oneline --decorate --graph --all
git show

